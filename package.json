{
  "name": "geoquilt",
  "version": "1.0.0",
  "description": "Geocoding exercise",
  "scripts": {
    "build": "rm -rf public && mkdir public && cp ./site/index.html ./public/index.html",
    "env": "export $(cat .env | xargs) && env",
    "test": "tape 'tests/?*/*' | tap-spec",
    "deploy": "now",
    "start": "now dev"
  },
  "devDependencies": {
    "now": "^16.2.0",
    "tap-spec": "^5.0.0",
    "tape": "^4.11.0"
  },
  "dependencies": {
    "dotenv": "^8.1.0",
    "haversine": "^1.1.1",
    "node-geocoder": "^3.24.0"
  },
  "author": "Drew Miller",
  "license": "ISC"
}
